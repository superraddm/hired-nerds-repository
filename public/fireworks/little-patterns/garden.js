(function(){
  'use strict';
  const $=id=>document.getElementById(id),L=window.GardenLearning,LP=window.LP;
  const esc=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const apples=n=>n+(n===1?' apple':' apples'),apple='<i class="apple" aria-hidden="true"></i>',shape=s=>'<i class="shape '+s+'" aria-hidden="true"></i>';
  const modes=['count','add','words','patterns'],wordTabs=['sentence','letter','order','numbers'];
  let mode='count',wordTab='sentence',indices={},rounds={},selection={},cursor={start:0,end:0};
  const pictureGlyphs={sun:'☀',tree:'🌳',ball:'⚽',cat:'🐈',dog:'🐕',bus:'🚌',fish:'🐟',moon:'🌙',flower:'🌼'};
  function letters(value){return LP.prefs.case==='lower'?String(value).toLowerCase():String(value).toUpperCase();}
  function current(){return rounds[mode==='words'?wordTab:mode];}
  function roundPrefs(kind){const p={...LP.prefs};if(selection[kind]>p.range)p.range=selection[kind];if(kind==='numbers'&&selection.numbers>p.range)p.range=selection.numbers;return p;}
  function createRound(kind){const index=indices[kind]||0,p=roundPrefs(kind);if(kind==='count')return L.countRound(selection.count?selection.count-1:index,p);if(kind==='add'){const r=L.sumRound(index,p);if(Array.isArray(selection.add)){r.a=selection.add[0];r.b=selection.add[1];r.total=r.a+r.b;r.choices=L.options(r.total,Array.from({length:Math.max(p.range,r.total)},(_,i)=>i+1),3,index);}return r;}if(kind==='patterns')return L.patternRound(index,p);if(kind==='sentence')return L.sentenceRound(index,p);if(kind==='letter')return L.letterRound(index,p);if(kind==='order')return L.orderRound(index,p);if(kind==='numbers')return L.numberWordRound(selection.numbers?selection.numbers-1:index,p);return null;}
  function save(){LP.storage.set('garden-position',{mode,wordTab,indices,selection});LP.storage.set('garden-rounds',rounds);}
  function restore(){const saved=LP.storage.get('garden-position',{});indices={count:0,add:0,patterns:0,sentence:0,letter:0,order:0,numbers:0};selection={};if(saved&&typeof saved==='object'){if(modes.includes(saved.mode))mode=saved.mode;if(wordTabs.includes(saved.wordTab))wordTab=saved.wordTab;Object.keys(indices).forEach(k=>{if(Number.isSafeInteger(saved.indices?.[k])&&saved.indices[k]>=0)indices[k]=saved.indices[k]%10000;});for(const key of ['count','numbers'])if(Number.isInteger(saved.selection?.[key])&&saved.selection[key]>=1&&saved.selection[key]<=10)selection[key]=saved.selection[key];const add=saved.selection?.add;if(Array.isArray(add)&&add.length===2&&add.every(n=>Number.isInteger(n)&&n>=1)&&add[0]+add[1]<=10)selection.add=add;}
    rounds={};const old=LP.storage.get('garden-rounds',{});for(const kind of Object.keys(indices)){const r=createRound(kind),s=old&&old[kind];if(s&&typeof s==='object'){const same=kind==='count'?s.target===r.target:kind==='add'?s.a===r.a&&s.b===r.b:kind==='sentence'||kind==='order'?s.text===r.text:kind==='letter'?s.word===r.word&&s.position===r.position:kind==='numbers'?s.target===r.target:JSON.stringify(s.sequence)===JSON.stringify(r.sequence);if(same){r.done=s.done===true;if(kind==='count'&&Array.isArray(s.seen))r.seen=[...new Set(s.seen.filter(i=>Number.isInteger(i)&&i>=0&&i<r.target))];if(kind==='add')r.stage=s.stage==='joined'?'joined':'parts';if('draft' in r)r.draft=typeof s.draft==='string'?s.draft.slice(0,500):'';if(kind==='order'){const used=Array.isArray(s.used)?s.used.filter(i=>Number.isInteger(i)&&i>=0&&i<r.tiles.length):[];r.used=used.every((tile,n)=>r.tiles[tile]===r.words[n])&&new Set(used).size===used.length?used:[];r.done=r.used.length===r.words.length;}}}rounds[kind]=r;}cursor={start:0,end:0};}
  function frame(target,seen=[],interactive=false){const size=target>5?10:5;return '<div class="five-frame" '+(!interactive?'role="img" aria-label="'+apples(target)+'"':'aria-label="Count the apples"')+'>'+Array.from({length:size},(_,i)=>{const tag=interactive&&i<target?'button':'span',counted=seen.indexOf(i);return '<'+tag+' class="pocket '+(i<target?'full':'')+(counted>=0?' counted':'')+'"'+(interactive&&i<target?' data-fruit="'+i+'" aria-label="Apple '+(i+1)+(counted>=0?', counted':'')+'"':'')+'>'+(i<target?apple:'')+(counted>=0?'<b class="count-tag">'+(counted+1)+'</b>':'')+'</'+tag+'>';}).join('')+'</div>';}
  function choiceButtons(values,word=false){return '<div class="choices '+(word?'word-options':'')+'">'+values.map(v=>'<button class="choice '+(word?'word-choice':'')+'" data-choice="'+esc(v)+'" aria-label="Choose '+esc(v)+'">'+esc(word?letters(v):v)+'</button>').join('')+'</div>';}
  function picture(name,label){return '<div class="word-picture" '+(name?'role="img" aria-label="'+esc(label)+'"':'aria-hidden="true"')+'>'+(name==='apple'?apple:name?pictureGlyphs[name]||'':'')+'</div>';}
  function header(label,speech,title,instruction,support){$('scene-label').textContent=label;$('speech').textContent=speech;$('activity-title').textContent=title;$('instruction').textContent=instruction;$('support').textContent=support;}
  function footer(help,next,done){$('help').hidden=false;$('help').textContent=help;$('next').textContent=next+' →';$('next').hidden=!done;$('repeat').hidden=!done;$('repeat').textContent='↻ Again';}
  function countView(){const r=current();header('THE PICNIC PATCH','Apples for our picnic!','How many apples?','Look in Nook’s picnic basket.','Choose any number');$('task').innerHTML=frame(r.target,r.seen,true)+(r.done?'<p class="result-note">✓ '+r.target+' '+(r.target===1?'apple':'apples')+' altogether.</p>':'<p class="choice-label">Tap their number.</p>'+choiceButtons(r.choices));footer('◎ Count with me','Next picnic',r.done);LP.status(r.done?'All in place. Repeat or choose another picnic.':'Look. Count. Choose.');}
  function keypad(){return '<div class="keyboard keypad" aria-label="Number keypad">'+Array.from('1234567890',c=>'<button class="key" data-pad="'+c+'">'+c+'</button>').join('')+'<button class="key function" data-pad="Backspace" aria-label="Delete the last digit">⌫ Delete</button><button class="key go" id="check-sum">Check ✓</button></div>';}
  function addView(){const r=current();header('ONE MORE FOR THE PICNIC','Let’s put them together!','Two groups. How many?','Each apple stays an apple.','Pick any sum');let html='<div class="sum-build"><div class="number-group"><span class="group-label">ALREADY HERE</span><div class="apples" role="img" aria-label="'+apples(r.a)+'">'+apple.repeat(r.a)+'</div><strong>'+r.a+'</strong></div><b class="operator">+</b><div class="number-group new"><span class="group-label">MORE APPLES</span><div class="apples" role="img" aria-label="'+apples(r.b)+'">'+apple.repeat(r.b)+'</div><strong>'+r.b+'</strong></div></div>';
    if(r.stage==='parts')html+='<button class="next" id="join">Put them together ↓</button>';else{html+='<div class="join-arrow" aria-hidden="true">↓</div><div class="sum-result"><div class="apples" role="img" aria-label="'+apples(r.total)+' together">'+apple.repeat(r.a)+'<span class="new-fruit">'+apple.repeat(r.b)+'</span></div>'+(r.done?'<strong>'+r.total+'</strong>':'')+'</div><p class="equation"><span>'+r.a+'</span> + <span>'+r.b+'</span> = '+(r.done?r.total:LP.prefs.addition==='type'?'<b class="typed" aria-label="Your answer">'+(esc(r.draft)||'?')+'</b>':'?')+'</p>'+(r.done?'<p class="choice-label">'+r.total+' altogether.</p>':LP.prefs.addition==='type'?'<p class="choice-label">Type how many altogether.</p>'+keypad():'<p class="choice-label">Tap how many altogether.</p>'+choiceButtons(r.choices));}$('task').innerHTML=html;footer(r.stage==='parts'?'◎ Show me':'↻ Show me again','Another little sum',r.done);LP.status(r.done?'The two groups make '+r.total+'.':r.stage==='parts'?'Tap Put them together when you are ready.':LP.prefs.addition==='type'?'Count the joined apples, then type their number.':'Count the joined apples, then choose their number.');}
  function patternView(){const r=current();header('THE MAKING PATCH','Let’s make a pattern!','One bead is missing','Help Nook finish the bead string.','Every pattern is open');$('task').innerHTML='<div class="pattern-strip" aria-label="Repeating bead pattern">'+r.sequence.map((s,i)=>'<span class="bead-slot '+(i===5&&!r.done?'missing':'')+'" aria-label="'+(i===5&&!r.done?'Missing bead':s)+'">'+(i===5&&!r.done?'?':shape(s))+'</span>').join('')+'</div><p class="repeat-label">'+r.unit.map(s=>s[0].toUpperCase()+s.slice(1)).join('. ')+'. Repeat.</p>'+(r.done?'<p class="result-note">✓ The pattern fits.</p>':'<div class="choices pattern-choices">'+r.choices.map(s=>'<button class="choice" data-choice="'+s+'" aria-label="Choose '+s+'">'+shape(s)+'</button>').join('')+'</div>');footer('◎ Show the pattern','Another pattern',r.done);LP.status(r.done?'All in place. You can play it again.':'Look for what repeats. Choose the missing piece.');}
  function keyboard(){return '<div class="keyboard" aria-label="Alphabet keyboard">'+Array.from('ABCDEFGHIJKLMNOPQRSTUVWXYZ',c=>'<button class="key" data-key="'+c+'">'+letters(c)+'</button>').join('')+'<span class="key blank"></span><button class="key function" data-key="Backspace" aria-label="Delete previous character">⌫ Delete</button><button class="key space" data-key="Space">Space</button></div>';}
  function typing(draft){return '<label class="word-label" for="word-input">Type the word</label><textarea id="word-input" class="word-board" maxlength="500" rows="2" spellcheck="false" autocapitalize="off" autocomplete="off" autocorrect="off" '+(LP.prefs.keyboard==='az'?'readonly inputmode="none"':'')+'>'+esc(draft)+'</textarea><div class="typing-actions"><button class="keyboard-toggle" id="keyboard-toggle">'+(LP.prefs.keyboard==='az'?'Use device keyboard':'Use A–Z keys')+'</button></div>'+(LP.prefs.keyboard==='az'?keyboard():'');}
  // Sentence tiles: the gap is a dashed slot until it is filled.
  function sentence(words,gapAt,filled,fillWith){return '<div class="sentence" aria-label="'+esc(words.map((w,i)=>i===gapAt&&!filled?'blank':w).join(' ').toLowerCase())+'">'+words.map((w,i)=>i===gapAt?(filled?'<span class="word-tile filled">'+esc(letters(fillWith||w))+'</span>':'<span class="word-tile gap">?</span>'):'<span class="word-tile">'+esc(letters(w))+'</span>').join('')+'<span class="word-tile stop" aria-hidden="true">.</span></div>';}
  function wordsView(){const r=current();let html='<div class="word-tabs" aria-label="Word activities">'+[['sentence','Missing word'],['letter','Missing letter'],['order','Word order'],['numbers','Number words']].map(([id,label])=>'<button data-word-tab="'+id+'" aria-pressed="'+(wordTab===id)+'">'+label+'</button>').join('')+'</div>';
    if(wordTab==='sentence'){header('THE WORD GARDEN','Which word is missing?','Finish the sentence','The picture is a clue.','One word at a time');html+=picture(r.picture,r.gap.toLowerCase())+sentence(r.words,r.gapAt,r.done)+(r.done?'<p class="result-note">✓ '+esc(letters(r.words.join(' ')))+'.</p><div class="word-tools"><button id="hear-model">♪ Hear the sentence</button></div>':'<p class="choice-label">Tap the missing word.</p>'+choiceButtons(r.choices,true));footer('◎ Give me a clue','Another sentence',r.done);LP.status(r.done?'The sentence is complete.':'Read the sentence. Look at the picture. Choose the word.');}
    else if(wordTab==='letter'){header('THE WORD GARDEN','A letter is hiding!','Find the missing letter','Spell the picture word.','One letter at a time');const tiles=Array.from(r.word).map((c,i)=>c===' '?'<span class="letter-tile space" aria-hidden="true"></span>':i===r.position&&!r.done?'<span class="letter-tile gap" aria-label="missing letter">?</span>':'<span class="letter-tile '+(i===r.position?'filled':'')+'">'+esc(letters(c))+'</span>').join('');html+=picture(r.picture,r.word.toLowerCase())+(r.picture?'':'<p class="choice-label">'+esc('A familiar word')+'</p>')+'<div class="letter-tiles" aria-label="'+esc(r.done?r.word.toLowerCase():'word with a missing letter')+'">'+tiles+'</div>'+(r.done?'<p class="result-note">✓ '+esc(letters(r.word))+'</p>':'<p class="choice-label">Tap the missing letter.</p>'+choiceButtons(r.choices,true))+'<div class="word-tools"><button id="hear-model">♪ Hear the word</button></div>';footer('◎ Give me a clue','Another word',r.done);LP.status(r.done?'The word is complete.':'Which letter fits the gap?');}
    else if(wordTab==='order'){header('THE WORD GARDEN','Let’s build a sentence!','Put the words in order','Tap the words, first to last.','Every sentence is open');const placed=r.used.map(i=>r.tiles[i]);html+=picture(r.picture,r.words.join(' ').toLowerCase())+'<div class="sentence" aria-label="'+esc(placed.join(' ').toLowerCase()||'empty sentence')+'">'+r.words.map((w,i)=>i<placed.length?'<span class="word-tile filled">'+esc(letters(placed[i]))+'</span>':'<span class="word-tile gap">'+(i===placed.length?'?':'')+'</span>').join('')+'<span class="word-tile stop" aria-hidden="true">.</span></div>'+(r.done?'<p class="result-note">✓ '+esc(letters(r.words.join(' ')))+'.</p>':'<p class="choice-label">Which word comes next?</p><div class="choices word-options">'+r.tiles.map((w,i)=>r.used.includes(i)?'':'<button class="choice word-choice" data-tile="'+i+'" aria-label="Choose '+esc(w)+'">'+esc(letters(w))+'</button>').join('')+'</div>')+'<div class="word-tools"><button id="hear-model">♪ Hear the sentence</button></div>';footer('◎ Give me a clue','Another sentence',r.done);LP.status(r.done?'The sentence is in order.':placed.length?placed.length+' of '+r.words.length+' words in place.':'Tap the first word.');}
    else{const variant=LP.prefs.numberWords;header('WORDS AND QUANTITIES','A number has a word!','How many apples?','A quantity, a number, and its word.','Choose any number');html+=frame(r.target);if(variant==='show'||r.done)html+='<div class="number-model"><strong>'+r.target+'</strong><span>'+letters(r.word)+'</span></div>';else html+='<div class="number-total">'+r.target+'</div>';if(variant==='show')html+='<button id="hear-model" class="help">♪ Hear the number word</button>';else if(variant==='choose'&&!r.done)html+=choiceButtons(r.choices,true);else if(variant==='type')html+=typing(r.draft)+'<div class="word-tools"><button class="next" id="check-word">Check my word</button></div>';footer('◎ Show me','Another number',variant==='show'||r.done);$('repeat').hidden=variant==='show';LP.status(variant==='show'?apples(r.target)+'. '+letters(r.word)+'.':r.done?'✓ The word matches the quantity.':'Choose or type the word for this number.');}
    $('task').innerHTML=html;bindInput();
  }
  function render(){LP.audio.stopSpeech();$('scene').classList.toggle('word-scene',mode==='words');document.querySelectorAll('[data-mode]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.mode===mode)));if(mode==='count')countView();else if(mode==='add')addView();else if(mode==='patterns')patternView();else wordsView();save();}
  function focusTask(){const target=$('task').querySelector('button,textarea');if(target)target.focus({preventScroll:true});}
  function setMode(value){if(!modes.includes(value))return;mode=value;render();}
  document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>setMode(b.dataset.mode));
  function setDraft(value){current().draft=value;current().done=false;save();}
  function rememberCursor(){const input=$('word-input');if(input)cursor={start:input.selectionStart,end:input.selectionEnd};}
  function bindInput(){const input=$('word-input');if(!input)return;cursor={start:input.value.length,end:input.value.length};input.addEventListener('input',()=>{setDraft(input.value);rememberCursor();$('next').hidden=true;$('repeat').hidden=true;});['select','click','keyup','blur'].forEach(event=>input.addEventListener(event,rememberCursor));}
  function typeKey(key){const input=$('word-input');if(!input)return;const text=key.length===1?letters(key):key;const result=L.editText(input.value,cursor.start,cursor.end,text);input.value=result.value;setDraft(result.value);cursor={start:result.caret,end:result.caret};input.setSelectionRange(result.caret,result.caret);$('next').hidden=true;$('repeat').hidden=true;LP.status('');}
  function typePad(key){const r=current();if(!r||r.stage!=='joined'||r.done)return;r.draft=key==='Backspace'?r.draft.slice(0,-1):(r.draft+key).slice(0,2);save();const typed=$('task').querySelector('.typed');if(typed)typed.textContent=r.draft||'?';LP.status('');}
  function checkSum(){const r=current();if(!r||r.stage!=='joined'||r.done)return;if(!r.draft){LP.status('Type a number first.');return;}if(Number(r.draft)!==r.total){LP.status('Count all the apples together. You can try again.');return;}r.done=true;render();LP.status('✓ '+r.total+' altogether.');$('next').focus({preventScroll:true});}
  function checkWord(){const r=current();if(!r||!L.matches(r.draft,r.word)){LP.status('The word is '+letters(r.word)+'. You can keep typing.');save();return;}r.done=true;$('next').hidden=false;$('repeat').hidden=false;LP.status('✓ Your word matches.');save();}
  function speakCurrent(){const r=current();if(wordTab==='numbers')LP.audio.speak(r.word);else if(wordTab==='letter')LP.audio.speak(r.word);else LP.audio.speak(r.words.join(' ').toLowerCase()+'.');}
  function choose(value){const r=current();const answer=mode==='count'?r.target:mode==='add'?r.total:mode==='patterns'?r.answer:wordTab==='sentence'?r.gap:wordTab==='letter'?r.letter:r.word;if(String(value)!==String(answer)){LP.status(mode==='patterns'?'Look at the repeating pair. You can try another piece.':mode==='words'?(wordTab==='sentence'?'Read the sentence again. The picture is a clue.':wordTab==='letter'?'Look at the letters around the gap. You can try again.':'Look at the number and its apples. You can try again.'):'Look at the apples. You can count them and try again.');return;}r.done=true;render();LP.status(mode==='patterns'?'✓ The pattern fits.':mode==='words'?(wordTab==='sentence'?'✓ The sentence is complete.':wordTab==='letter'?'✓ The word is complete.':'✓ The word matches the number.'):'✓ '+answer+' altogether.');$('next').focus({preventScroll:true});}
  function placeTile(index){const r=current();if(r.done||r.used.includes(index))return;if(r.tiles[index]!==r.words[r.used.length]){LP.status('Not that one yet. Which word comes '+(r.used.length?'next':'first')+'?');return;}r.used.push(index);r.done=r.used.length===r.words.length;render();if(r.done){LP.status('✓ The sentence is in order.');$('next').focus({preventScroll:true});}else focusTask();}
  function countFruit(index){const r=current();if(!r.seen.includes(index))r.seen.push(index);countView();LP.status(r.seen.length===r.target?r.target+' '+(r.target===1?'apple':'apples')+' altogether.':r.seen.length+' counted.');save();}
  function join(){const r=current();r.stage='joined';r.done=LP.prefs.addition==='show';render();focusTask();}
  function hint(selector){$('task').querySelectorAll('.choice').forEach(b=>b.classList.toggle('hint',b.matches(selector)));}
  function help(){const r=current();if(mode==='count'){const unseen=Array.from({length:r.target},(_,i)=>i).find(i=>!r.seen.includes(i));if(unseen!==undefined)countFruit(unseen);else{LP.status(apples(r.target)+' altogether. Choose '+r.target+'.');hint('[data-choice="'+r.target+'"]');}}
    else if(mode==='add'){if(r.stage==='parts')join();else{r.stage='parts';r.done=false;render();}}
    else if(mode==='patterns'){$('task').querySelectorAll('.bead-slot').forEach((el,i)=>el.classList.toggle('emphasis',i<r.unit.length));LP.status('This little group repeats: '+r.unit.join(', ')+'.');}
    else if(wordTab==='sentence'){if(r.done)speakCurrent();else{LP.status('The picture shows: '+r.gap.toLowerCase()+'.');hint('[data-choice="'+r.gap+'"]');}}
    else if(wordTab==='letter'){if(r.done)speakCurrent();else{LP.status('The word is '+letters(r.word)+'. The hidden letter is '+letters(r.letter)+'.');hint('[data-choice="'+r.letter+'"]');}}
    else if(wordTab==='order'){if(r.done)speakCurrent();else{const next=r.words[r.used.length];LP.status('The '+(r.used.length?'next':'first')+' word is '+letters(next)+'.');const tile=r.tiles.findIndex((w,i)=>w===next&&!r.used.includes(i));hint('[data-tile="'+tile+'"]');}}
    else{LP.status(r.target+' is written '+letters(r.word)+'.');hint('[data-choice="'+r.word+'"]');}save();}
  $('task').addEventListener('pointerdown',e=>{if(e.target.closest('[data-key]')){rememberCursor();e.preventDefault();}});
  $('task').addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.wordTab){wordTab=b.dataset.wordTab;render();return;}if(b.dataset.key){typeKey(b.dataset.key);return;}if(b.dataset.pad){typePad(b.dataset.pad);return;}if(b.dataset.fruit!==undefined){countFruit(Number(b.dataset.fruit));return;}if(b.dataset.tile!==undefined){placeTile(Number(b.dataset.tile));return;}if(b.dataset.choice!==undefined){choose(b.dataset.choice);return;}switch(b.id){case 'join':join();break;case 'check-sum':checkSum();break;case 'hear-model':speakCurrent();break;case 'check-word':checkWord();break;case 'keyboard-toggle':{const next=LP.prefs.keyboard==='az'?'device':'az';LP.savePrefs({...LP.prefs,keyboard:next});if(next==='device')$('word-input')?.focus();break;}}});
  $('help').onclick=help;$('next').onclick=()=>{const key=mode==='words'?wordTab:mode;delete selection[key];indices[key]++;rounds[key]=createRound(key);render();focusTask();};$('repeat').onclick=()=>{const key=mode==='words'?wordTab:mode;rounds[key]=createRound(key);render();focusTask();};
  function choosePuzzle(){const el=LP.openModal('<div class="lp-dialog-heading"><h2 id="dialog-title">Choose any puzzle</h2><button data-close aria-label="Close puzzle chooser">×</button></div><p>Everything is open. Pick what you want to explore.</p><div id="picker-content"></div>');const content=el.querySelector('#picker-content');const key=mode==='words'?wordTab:mode;
    if(['count','numbers'].includes(key)){content.className='puzzle-picker';for(let n=1;n<=10;n++){const b=document.createElement('button');b.textContent=n;b.onclick=()=>{selection[key]=n;rounds[key]=createRound(key);LP.closeModal();render();};content.appendChild(b);}}
    else if(key==='add'){content.innerHTML='<form id="pick-sum"><label>First group<select name="a"></select></label><label>Second group<select name="b"></select></label><p>Choose groups with a total up to 10.</p><p id="sum-error" role="status"></p><button class="lp-primary">Play this sum</button></form>';const form=content.querySelector('form');for(const field of ['a','b'])for(let n=1;n<=9;n++){const opt=document.createElement('option');opt.value=n;opt.textContent=n;form.elements[field].appendChild(opt);}form.onsubmit=e=>{e.preventDefault();const a=Number(form.elements.a.value),b=Number(form.elements.b.value);if(a+b>10){el.querySelector('#sum-error').textContent='This set goes up to 10. Choose smaller groups.';return;}selection.add=[a,b];rounds.add=createRound('add');LP.closeModal();render();};}
    else if(key==='patterns'){content.className='puzzle-picker';for(let i=0;i<6;i++){const b=document.createElement('button');const sample=L.patternRound(i,{...LP.prefs,choices:3});b.textContent=sample.unit.map(s=>({circle:'●',square:'■',triangle:'▲'}[s])).join(' ');b.setAttribute('aria-label',sample.unit.join(', ')+' repeating');b.onclick=()=>{LP.savePrefs({...LP.prefs,choices:3});indices.patterns=i;rounds.patterns=createRound('patterns');LP.closeModal();render();};content.appendChild(b);}}
    else if(key==='letter'){content.className='puzzle-picker';const words=L.PICTURES.concat(LP.prefs.customWords.map(word=>({word})));words.forEach((w,i)=>{const b=document.createElement('button');b.textContent=letters(w.word);b.style.fontSize='16px';b.onclick=()=>{indices.letter=i;rounds.letter=createRound('letter');LP.closeModal();render();};content.appendChild(b);});}
    else{content.className='puzzle-picker';L.SENTENCES.forEach((s,i)=>{const b=document.createElement('button');b.textContent=letters(s.text);b.style.fontSize='14px';b.onclick=()=>{indices[key]=i;rounds[key]=createRound(key);LP.closeModal();render();};content.appendChild(b);});}
  }
  $('choose-puzzle').onclick=choosePuzzle;
  let oldPrefs={...LP.prefs};document.addEventListener('lp:preferences',()=>{const changed=['range','choices','addition','numberWords','customWords'].some(k=>JSON.stringify(oldPrefs[k])!==JSON.stringify(LP.prefs[k]));if(changed){selection={};Object.keys(indices).forEach(k=>{rounds[k]=createRound(k);});}oldPrefs={...LP.prefs};render();});
  document.addEventListener('lp:beforeplayerchange',save);document.addEventListener('lp:playerchanged',()=>{mode='count';wordTab='sentence';restore();oldPrefs={...LP.prefs};render();LP.status('Ready to play. All activities are open.');});window.addEventListener('pagehide',save);
  // Hardware keys: digits answer the addition keypad, letters pick a missing-letter choice, and typing feeds the number-word textarea while the A–Z keys are shown.
  document.addEventListener('keydown',e=>{if(LP.dialogOpen||e.ctrlKey||e.metaKey||e.altKey||e.isComposing)return;if(mode==='add'&&LP.prefs.addition==='type'&&$('check-sum')){if(/^[0-9]$/.test(e.key)||e.key==='Backspace'){e.preventDefault();typePad(e.key);}else if(e.key==='Enter'&&e.target.tagName!=='BUTTON'){e.preventDefault();checkSum();}return;}if(mode==='words'&&wordTab==='letter'&&/^[a-z]$/i.test(e.key)){const b=$('task').querySelector('[data-choice="'+e.key.toUpperCase()+'"]');if(b){e.preventDefault();b.click();}return;}if(mode!=='words'||LP.prefs.keyboard!=='az'||!$('word-input'))return;if(e.target.tagName==='BUTTON'&&[' ','Enter'].includes(e.key))return;if(e.key==='Backspace'||e.key.length===1){e.preventDefault();typeKey(e.key===' '?'Space':e.key);}});
  restore();const requested=location.hash.slice(1);if(modes.includes(requested))mode=requested;render();
})();
