// Reproducible public vocabulary reference and private art review. No player data.
const fs=require('node:fs'),path=require('node:path');
const F=require('../public/fireworks/little-patterns/feelings-data.js');
const A=require('../public/fireworks/little-patterns/feelings-art.js');
F.validate();
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const style='body{font:18px/1.6 "Trebuchet MS",Arial,sans-serif;background:#fffbe7;color:#253e59;margin:0}main{max-width:1150px;margin:auto;padding:25px}h1,h2{line-height:1.25}a{color:#345980;display:inline-flex;align-items:center;min-height:48px}p{max-width:80ch}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:16px}.card{background:#fffef4;border:2px solid #bac5a4;border-radius:20px;padding:12px;text-align:center}.card svg{width:100%;max-height:230px;display:block}td .story-art{width:220px;height:auto}table{border-collapse:collapse;background:#fffef4}th,td{padding:12px;border:1px solid #a4b39a;text-align:left;vertical-align:top}.table{overflow:auto}.pair{display:flex}.pair svg{width:50%;height:240px}details{margin:24px 0}summary{cursor:pointer;min-height:48px;font-size:1.3rem}a:focus-visible,summary:focus-visible{outline:4px solid #6c4892;outline-offset:3px}';
const page=(title,body)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><style>${style}</style></head><body><main>${body}</main></body></html>\n`;
const cards=F.PEOPLE.flatMap(p=>F.FEELINGS.map(f=>`<div class="card">${A.svg(p.id,f)}<strong>${p.name} · ${F.WORDS[f]}</strong></div>`)).join('');
const html=page('Feelings: all pictures and words',`<a href="feelings.html">Back to Feelings</a><h1>All Feelings content</h1><p>Generated from feelings-data.js and feelings-art.js. These pictures show possible expressions. They cannot tell us what a real person feels. All ${F.FEELINGS.length} words and every activity are open.</p><h2>The ${F.FEELINGS.length} words</h2><p>${F.FEELINGS.map(f=>F.WORDS[f]).join(' · ')}. Me also offers Not sure and Something else. Every Me choice is accepted and stays only on that screen.</p><h2>All ${F.STORIES.length} People stories</h2><p>The person names their feeling from the start. Hear repeats the situation and their words. These individual stories do not imply everyone feels the same way in that situation.</p><div class="table"><table><thead><tr><th>Context</th><th>Scene</th><th>What the person says</th></tr></thead><tbody>${F.STORIES.map(s=>`<tr><td>${esc(s.context)}</td><td>${A.scene(s)}</td><td>${esc(F.statement(s))}</td></tr>`).join('')}</tbody></table></div><details><summary>All ${F.PEOPLE.length*F.FEELINGS.length} Match pictures</summary><div class="cards">${cards}</div></details><h2>Match responses</h2><p>These openers rotate independently of the pictured feeling: ${F.MATCH_OPENERS.map(esc).join(" / ")}. Each is followed by the pictured person?s name and feeling.</p><details><summary>All 40 character and feeling statements</summary><ul>${F.PEOPLE.flatMap(p=>F.FEELINGS.map(f=>`<li>${esc(F.matchStatement(p.id,f))}</li>`)).join('')}</ul></details><h2>How Match works</h2><p>Find an identical picture from two choices of the same person. A different picture reveals its word and invites another look. Next is always available. After a match, copying the pose is optional and never measured. Choosing a word in Menu keeps the next pictures on that word; All feelings mixes them again.</p><h2>Art and voice</h2><p>Original human illustrations, no TV characters or photos. The Jenny (Dioco) voice is generated locally before publication. No live speech service or LLM is used.</p><p><a href="assets/feelings/NOTICE.txt">Art provenance</a> · <a href="assets/voice/NOTICE.txt">Voice provenance</a> · <a href="content.html">Other game content</a></p>`);
fs.writeFileSync(path.join(__dirname,'../public/fireworks/little-patterns/feelings-content.html'),html);
const review=page('Feelings: gentle expression and pose review',`<h1>Everyday stories: art review</h1><div class="cards">${F.STORIES.map(s=>`<div class="card">${A.scene(s)}<p>${esc(s.context)}</p><strong>${esc(F.statement(s))}</strong></div>`).join('')}</div><h2>Sad and angry</h2><p>Local build review. Each person has every feeling in the game. These mild expressions are examples, not rules. Pose copying is always optional; there is no camera, score or required movement. Nothing has been deployed.</p><div class="cards">${F.PEOPLE.flatMap(p=>['sad','angry'].map(f=>`<div class="card"><strong>${p.name} · ${F.WORDS[f]}</strong><div class="pair">${A.svg(p.id,f)}${A.svg(p.id,f,{pose:true})}</div></div>`)).join('')}</div><h2>All expressions</h2><div class="cards">${cards}</div>`);
fs.writeFileSync(path.join(__dirname,'../docs/nook-feelings-art-review.html'),review);
console.log('Built '+F.STORIES.length+' stories, '+(F.PEOPLE.length*F.FEELINGS.length)+' picture references and eight portrait/pose review pairs.');
